const numeros = [100,200,300,400,500,600]
//for ela fica no loop até chegar onde foi citada(tomar cuidado com o loop infinito) laço de repetição
for (let i = 0; i < numeros.length; i++){
    console.log(i, numeros[i])
}
